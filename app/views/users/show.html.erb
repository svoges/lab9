<h1><%= @user.name %> <% if @user['id'] == current_user['id'] %> <%= link_to '+', new_quit_path(user_id: @user) %> <% end %></h1>
<% @user.quits.each do |quit| %>
  <p><%= quit.text %>  <%= link_to 'Edit', edit_quit_path(quit) %></p>
<% end %>
